# Migration ledger — cffdrs (R) → gofbp (Go)

What has been read, what has been ported, what has deliberately been left out,
and what is still owed. The upstream is
[cffdrs/cffdrs_r](https://github.com/cffdrs/cffdrs_r); the daily procedure that
keeps this file honest is [DAILY-CHECK.md](DAILY-CHECK.md). `/migration-check`
audits this file daily; `/migration-sweep` is the same audit's judgement half fanned
out, for when ten minutes is not enough; `/migration-port` moves one row of it.

A row here is a claim about this repository, so it has to be checkable. "Ported"
means the Go function exists **and** a test asserts it; "Ported ✅" additionally
means a `TestCFFDRS*` asserts it against the fixture, which is the only status
that says the coefficients are *right* rather than merely self-consistent.

## Pins

| | value | checked |
|---|---|---|
| Upstream commit last read | `4d20a30` (2026-05-11) | 2026-09-24 |
| Upstream package version | 1.10.0 | 2026-09-24 |
| Oracle pins (`testdata/Dockerfile`) | cffdrs 1.9.2, R 4.6.1 | 2026-09-24 |
| Fixture sha256 | `e438b1d3…865387` (`testdata/README.md`) | 2026-09-24 |

> **Open drift — re-measured 2026-09-18, and it is not what this note used to
> say.** The oracle pins cffdrs 1.9.2 because **1.9.2 is CRAN's current release**.
> 1.10.0 exists only in the upstream git tree:
> `remotes::install_version('cffdrs', version = '1.10.0')` fails with *version
> '1.10.0' is invalid for package 'cffdrs'*, and the CRAN archive runs 1.7 … 1.9.0
> with 1.9.2 current and no 1.10.x at all. So this was never a bump nobody had got
> to — there is nothing released to bump to, and the oracle is already pinned to
> the newest cffdrs a user can install.
>
> That makes the question one of provenance rather than scheduling, and it is
> still a human's to answer: **taking reference numbers "from 1.10.0" means taking
> them from upstream's git HEAD instead of from a CRAN release**, which is a
> different claim about where this package's numbers come from.
>
> **It did not block the FMC port, and the reason is measured rather than
> assumed.** Both versions' source was read side by side on 2026-09-18 — 1.9.2's
> out of the pinned container, HEAD's from `4d20a30` — and they are the same
> arithmetic. HEAD splits `foliar_moisture_content` into two files, adds an
> `is.na` guard and an `FMCo` override, and warns outside North America; none of
> that moves a number. The one difference that *can*: 1.9.2's `round(D0, 0)` sits
> after the ifelse and so rounds a **caller-supplied** `D0` as well, where HEAD
> rounds only a computed one. A supplied `D0` of 150.7 is 151 to 1.9.2 and 150.7
> to HEAD. Integer dates — the only kind the sweep or a sane caller passes — are
> unaffected, and `foliar.go` reproduces 1.9.2 and says so. So the decision now has
> a named consequence attached to it instead of an open-ended one.
>
> **Nor does it block the FWI System (2026-09-24), and here there is not even an
> exception.** The seven FWI files were read side by side the same way — 1.9.2's
> function bodies printed from the pinned container, HEAD's fetched at `4d20a30`
> — and the arithmetic of all seven is identical, expression for expression,
> `fwi()`'s RH clamp and DSR line included.
>
> Checked while here: 1.9.2's `fbp(output = "ALL")` does return `D0`, but it is
> the **echoed input**, not the computed date of minimum — verified by sending
> `D0 = 150` and `D0 = 0` and reading the column back. So there is no oracle
> column for eqs. 1-4 on their own; they are asserted through FMC.

> **Open question (needs a decision, not a daily check):** what status
> `rate_of_spread_at_theta.r` should carry now. Both upstream rates at a bearing
> were measured against the pinned oracle on 2026-09-18 and both are defective —
> `fbp()`'s `TROS` mixes radians and degrees inside one `cos()`, and eq. 94
> collapses to `ROS/cosθ` with an uncaught pole at 90°. The row's own evidence
> therefore says 🟢, "no oracle column exists", which is where it started; what
> has changed is that the reason is now checked rather than assumed. It is left
> at 🟡 because moving it is a status call, and because the one thing still
> unchecked — whether the *published* eq. 94 reads the way cffdrs implements it —
> would change what the row is a finding *about*. Reading Wotton et al. (2009)
> eq. 94 is the next concrete step, and it needs the paper.
>
> **Open questions raised by the 2026-09-18 orchestrated sweep (`/migration-sweep`),
> all status or scope calls:**
>
>   Only one of its edges is real (see the note above that list), and item 1 —
>   CBH/CFL — is blocked on the exposure decision below rather than on any
>   dependency. The list is unchanged pending this.
> - **Reorder the dependency order?**
> - **Split `inst/` out of the `man/`, `inst/` ⚪ row?** `inst/extdata` ships
>   upstream's own FBP reference output, which is not "docs and package metadata".
>   Related: does upstream-published output count as a reference source here at
>   all, given it is not generated from the pinned container? That is the same
>   provenance question the 1.10.0 note parks above.
> - **`data/` 🟡 → ⚪?** Once the two wrong claims are removed, there is nothing in
>   it for gofbp to port.
> - **Narrow `TestCFFDRSCrownFractionBurned`'s C6 exclusion?** Its second pass
>   matches all 768 C6 crown rows exactly, so the exclusion currently hides
>   agreement. Narrowing an exclusion changes what a test asserts, so it is
>   reported, not done. **Still open after the 2026-09-19 C6 port**, which left
>   that test untouched on purpose and put C6's coverage in a test of its own.
> - **Should `Slopecalc.r` stay ✅ now that C6's sloped path is known to be
>   missing?** Found by the 2026-09-19 C6 port and written into that row below:
>   cffdrs back-solves the slope-equivalent wind from `rate_of_spread()`, which
>   for C6 returns the **blended** rate, so its equivalent wind for a sloped C6
>   stand moves with FMC and CBH. `NetEffectiveWind` is surface-only for every
>   fuel, so it cannot reproduce C6's sloped ISI, and `TestCFFDRSSlopeBackSolve`'s
>   C6 exclusion is load-bearing rather than cosmetic. The row's ✅ is true of the
>   sixteen fuels the test asserts and silent about the seventeenth; 🟡 is
>   arguable. A status call, so it is reported.
>
> **Open question (needs a decision, not a daily check):** the `direction.r` row
> below claims an equivalence that does not hold. The honest placement is ⚪,
> alongside the `lros.r`/`pros.r` rows whose helper it is, with `AngleBetweenDeg`
> recorded as gofbp's own helper rather than a port of anything. That is a move out
> of scope, so it is left for a human to make deliberately.

## Status key

| | meaning |
|---|---|
| ✅ | ported, and a `TestCFFDRS*` asserts it against the fixture |
| 🟢 | ported, asserted only by identity/invariant tests — no oracle column exists |
| 🟡 | partially ported — read the note, it is a real gap, not a rounding difference |
| 🔴 | not ported, and in scope |
| ⚪ | deliberately out of scope — the reason is the row's whole content |

## R/ — file by file

The module ships two packages, and this table covers both. A row whose gofbp
column names `fwi/…` is the FWI System, in package `fwi`; every other row is
package `fbp`. They share no code and neither imports the other — see the
2026-09-24 Log entry for why the FWI System came into scope as a package of its
own rather than as part of `fbp`. One upstream file backs a row in each:
`initial_spread_index.r` is FBP's ISI with `fbpMod = TRUE` and the FWI
System's with `fbpMod = FALSE`, so it has two rows, told apart by that variant
in the first column, and a test's `ledger:` marker names the one it asserts.

| Upstream file | Concept | Status | gofbp |
|---|---|---|---|
| `rate_of_spread.r` | RSI, all 17 fuel types; M1/M2 PC and M3/M4 PDF blends; O1 curing | ✅ | `fbp.go` `RSI` |
| `buildup_effect.r` | BE | ✅ | `fbp.go` `BuildupEffect` |
| `Slopecalc.r` | WSE/WSV/RAZ slope back-solve, SF | ✅ | `slopewind.go` — **✅ for sixteen fuels; C6's sloped path is missing, found 2026-09-19 while porting `C6calc.r`.** `slope_adjustment` takes the zero-wind rate it inverts from `rate_of_spread(FUELTYPE, ISZ, …)`, and `rate_of_spread` for C6 returns `rate_of_spread_extended(...)$ROS` — the **blended** rate. So cffdrs multiplies a blended zero-wind rate by SF and inverts C6's *surface* curve (a = 30, b = 0.08, c0 = 3) to recover an equivalent wind, which makes C6's equivalent wind a function of FMC, SFC and CBH. It is visible in the fixture on disk: at GS 30 / WS 0 / FFMC 95 / BUI 100, C6's `isi` column is 15.930431 at FMC 85.1701 and 11.328856 at FMC 120 — same weather, and it moves with foliar moisture and crown base height. `NetEffectiveWind` is surface-only for every fuel, so it reproduces none of that, and `TestCFFDRSSlopeBackSolve`'s C6 exclusion via `crownChangesROS` is what keeps this row's ✅ true. Porting C6's crown rate of spread did **not** lift it — the missing piece is in the back-solve, not the crown path. `TestCFFDRSC6RateOfSpread`'s end-to-end pass is flat-only for exactly this reason, and says so. Whether the row should move to 🟡 is a status call; see the open question above. |
| `initial_spread_index.r` (`fbpMod = TRUE`) | ISI with FBP's high-wind wind function — what `fbp()` computes | ✅ | `fbp.go` `ISI` — the `fbpMod = FALSE` path, the FWI System's own ISI, is a separate row below in package `fwi`. Same file, same name, different wind function above 40 km/h; both doc comments say so. |
| `length_to_breadth.r` | LB | ✅ | `ellipse.go` `LengthToBreadth` |
| `back_rate_of_spread.r` | BROS via the back ISI ratio | ✅ | `ellipse.go` `BackISIRatio` |
| `flank_rate_of_spread.r` | FROS | ✅ | `ellipse.go` `FlankROS` |
| `CFBcalc.r` | CSI, RSO, CFB, FD | ✅ | `crown.go` |
| `rate_of_spread_at_theta.r` | ROS at an arbitrary bearing | 🟡 | `ellipse.go` `ROSAtAngle` — **it became a finding.** The 2026-09-18 audit corrected this row from 🟢 (on the false claim that `fbp()` returns no rate at a bearing) to 🟡, and predicted "emit `TROS` from the generator and the row becomes ✅, or it becomes a finding". Measured against the pinned oracle on 2026-09-18 — cffdrs 1.9.2 / R 4.6.1, the container the fixture comes from — and **that correction was itself wrong in two ways.** (1) It conflated two different upstream quantities. `fbp()`'s `TROS` is `ROS·(1-E)/(1-E·cos(THETA-RAZ))`, `E = sqrt(1-1/LB²)` — built from ROS and LB only, never FROS or BROS. Eq. 94's `rate_of_spread_at_theta(ROS, FROS, BROS, THETA)` — the file this row is keyed on — is a different formula, is **unexported**, and is **never called by `fbp()`**. Only the second is the quantity `ROSAtAngle` implements. (2) **Neither is usable as a reference, because both are numerically defective.** `TROS`: `THETA` is converted to radians (`THETA <- THETA * pi/180`) and `RAZ` to degrees (`RAZ <- RAZ * 180/pi`), then subtracted inside one `cos()` — so `TROS` does not return `ROS` even at the head. The case, named so the next reader does not have to search for it: **C2, FFMC 90, BUI 60, WS 20, GS 0, WD 0 — at `THETA = RAZ = 180`, `TROS` = 2.835 against `ROS` = 16.149.** (Those are oracle outputs for that input, not fixture rows; the fixture emits no `theta` or `tros` column.) Present at the pinned upstream commit `4d20a30` too, and it propagates to `TCFB`, `TFI`, `TTI`, `TROSt` and `TTFC`. Eq. 94: its two leading terms `(ROS-BROS)/(2·cosθ)` and `(ROS+BROS)/(2·cosθ)` share a denominator and collapse to `ROS/cosθ`, so there is a pole at θ = 90° that the `ifelse(c1 == 0, …)` guard never catches (`cos(π/2)` is 6.12e-17, not 0) — it returns -2.34e17 at 90°, negative rates from 30° to 89.9°, and `ROS` at *both* 0° and 180°, so `BROS` is never recovered. `ROSAtAngle` on the same case returns `ROS` at 0° and 0.9674577 at 180°, which is `BROS` to seven figures. **Not checked: whether the published eq. 94 in Wotton et al. (2009) reads the same way** — that needs the paper, and it is the difference between a cffdrs transcription error and one in ST-X-3's successor. Consequence: ✅ is not one generator line away and may not be reachable at all. Emitting `TROS` would assert gofbp against an oracle bug, and under this repo's rules the mismatch would have read as gofbp's fault. Also note `theta` is fixed at `0` in all 24,260 cases (`base_row`), so any θ-dependent column needs the sweep widened — which moves the digest and the case count in four places. **Left 🟡 rather than moved: on this evidence the honest status is 🟢** (ported, no oracle column exists — the original status, for a reason that is finally true), but that is a status call reserved for a human. See the open question above. |
| `direction.r` | `.direction(bearingT1T2, bearingT1T3, ThetaAdeg)` — rotates a bearing by an offset, with quadrant handling; signed, roughly [-180, 180] | 🟢 | `ellipse.go` `AngleBetweenDeg` — **the equivalence was checked on 2026-09-06 and it is not real.** Upstream takes three arguments and rotates one bearing by an offset; ours takes two and returns their unsigned separation in [0, 180]. `.direction` is a helper of `pros()`/`lros()`, which are ⚪ below, so it has no caller on the FBP forward path at all. `AngleBetweenDeg` is a gofbp convenience with no upstream counterpart, asserted by `TestAngleBetweenDeg`. **Re-checked by query on 2026-09-18, and the 🟢 legend's "no oracle column exists" is false as stated for this row:** `pros()`/`lros()` are both *exported* and return `data.frame(Ros, Direction)`, where `Direction` is `.direction`'s output verbatim. It is simply not in `fbp()`'s 43 columns — the only direction-like one is `RAZ`, the head azimuth — so it is unreachable from this repo's fixture. It is also **not an oracle for `AngleBetweenDeg`**: `.direction(10,40,15)` and `.direction(40,10,15)` both return 25 where `AngleBetweenDeg(10,40)` is 30. 🟢 stands on the narrow, now-tested ground that nothing upstream computes what `AngleBetweenDeg` computes. If `pros`/`lros` are ever brought into scope, `Direction` needs the `TROS` treatment first: its wrap tail adds ±10 rather than ±360 (`.direction(170,160,-30)` → 190, where the correct wrap is -160) and equal bearings fall through every branch to `NA`. Reclassifying this row is a scope call — see the open question above. |
| `foliar_moisture_content.r`, `foliar_moisture_content_minimum.r` | FMC from lat/long/elev/date | ✅ | `foliar.go` `FoliarMoistureContent`, `DateOfMinimumFoliarMoisture` — eqs. 1-8, asserted over 17,364 cases at **worst relative error 0**. Oracle case (b): the `fmc` column existed, but `ELV`, `LONG` and `D0` were constants at 0, 15 and 0, so the elevation branch (`LATN = 43 + 33.7·exp(-0.0351·(150-LONG))`, `D0 = 142.1·LAT/LATN + 0.0172·ELV`) and the caller-supplied-`D0` branch had no coverage and eqs. 1/2's longitude exponential was pinned at one point. The sweep grew a 216-row FMC block (18 sites × 12 dates) and the `long`/`elv`/`d0` input columns to reach all three — now 7 longitudes on eqs. 1/2 and 6 elevations on eqs. 3/4. Both "details a port must not miss" flagged by the 2026-09-18 sweep are handled and now oracle-backed: `round(D0, 0)` is R's **half-to-even**, so `math.RoundToEven` and not `math.Round` — sites at `D0 = 150.5` and `151.5` separate the two rules and the oracle sides with half-to-even — and the single-longitude pin is gone. `Crown.FMC` is still the caller's field. The 6,384 D1/S1/S2/S3/O1A/O1B rows are excluded by name: `fbp()` zeroes FMC for the crownless fuels *after* computing it, so their column describes the driver, not eqs. 1-8. **One claim of that sweep is withdrawn.** `foliar_moisture_content_minimum` indeed does not exist at the pin — but "half this row is unportable against 1.9.2 by construction" does not follow. 1.9.2 carries eqs. 1-4 *inline* inside its single `foliar_moisture_content`, and that inline code is byte-for-byte the arithmetic HEAD moved out into the second file, so the whole concept was portable and both halves are asserted. What the file split genuinely changes is 1.9.2's rounding of a **supplied** `D0`, which HEAD dropped; see the drift note above. |
| `surface_fuel_consumption.r` | SFC per fuel from FFMC/BUI | ✅ | `consumption.go` `SurfaceFuelConsumption` — all eleven equations, asserted over all 24,260 cases. `Crown.SFC` is still the caller's field: this gives them something to fill it with, it does not fill it for them. |
| `crown_base_height.r` | per-fuel CBH defaults | 🔴 | — Caller must supply `Crown.CBH`. **There is no direct column but there is an oracle** (queried 2026-09-18): `fbp(output = "All")`'s 43 columns include neither CBH nor CFL, but sending `CBH = -1` and inverting `CSI` (eq. 56) recovers the published table verbatim — `2, 3, 8, 4, 18, 7, 10, 0, 6, 6, 6, 6, 0, 0, 0, 0, 0`. What blocks this row is the exposure decision below, not the absence of a reference. |
| `crown_fuel_load.r` | per-fuel CFL defaults | 🔴 | — Caller must supply `Crown.CFL`. This table is what keeps D1/S1–S3/O1A/O1B at zero crown; without it that behaviour is the caller's to get right. Same as CBH above: recoverable as `CFC/CFB` once the mixedwood weighting is divided out (`crown_fuel_consumption` applies `PC/100` to M1/M2 and `PDF/100` to M3/M4), giving `0.75, 0.8, 1.15, 1.2, 1.2, 1.8, 0.5, 0, 0.8, 0.8, 0.8, 0.8, 0, 0, 0, 0, 0`. The six zero entries are not recoverable by division but are pinned by the `CFL > 0` gate — those fuels come back `CFB = 0`. |
| `C6calc.r` | C6 crown ROS (RSC), CFB-blended final ROS | ✅ | `c6.go` `FoliarMoistureEffect`, `C6CrownROS`, `C6CrownFractionBurned`, `C6ROS`, `C6CrownFire` — eqs. 61, 64, 65 and eq. 58's C6 gate, asserted over all 948 C6 cases at **worst relative error 1.6e-15**. Oracle case **(a)**: no generator change, no regeneration, and the fixture is byte-identical — `tools/fixture-diff` over the pre-port copy reports *No column shared by both fixtures moved*, digest still `e0e51ff1…7db8cc`. The 2026-09-18 sweep's prediction held exactly: C6's `ros`/`cfb` columns already **are** the blended quantity, and `crown_rate_of_spread_c6`/`crown_fraction_burned_c6`/`rate_of_spread_c6` take `ISI, FMC, RSS, RSO, CFB` and no CBH/CFL table, so nothing was owed to the row above. Eqs. 62 (RSI = 30·(1−exp(−0.08·ISI))³) and 63 (RSS = RSI·BE) needed no code at all: they are the generic `a`/`b`/`c0` curve at C6's own table row, so `RSI("C6", …)·BuildupEffect("C6", …)` already returned them. Three passes, narrowing what a failure can mean — eq. 65 fed the oracle's own CFB over all 948 rows; the whole chain from the oracle's ISI over the 768 crown-block rows; and end to end from FFMC and wind over the 384 **flat** crown-block rows. **The end-to-end pass is flat-only, and that is a finding about `Slopecalc.r` rather than a thin sweep** — see that row. **Two things the sweep cannot reach, both said out loud in the tests rather than left to look covered.** (1) The `RSC > RSS` gate is never false: RSC exceeds RSS in all 948 rows, and that is a property of the equations and not of the sweep. RSS has a supremum of 30·BE_sup = 35.9149 m/min over all ISI and BUI (C6's q = 0.8, BUI0 = 62), RSC's is 60·FME/FMEavg, and the two cross at **FMC 127.26** — above the 120 eq. 8 caps foliar moisture at, so the gate is unreachable from the model's own FMC. `TestC6CrownGateIsUnreachableWithinTheFMCRange` carries the bound and the branch is asserted unconditionally instead. (2) cffdrs' `ROS <= 0 → 1e-6` floor: the smallest C6 rate in the sweep is 1.70913e-08, positive, so the floor never fires and `C6ROS` not reproducing it moves no number. **Three places the R departs from the paper, all now written down.** cffdrs' `crown_fraction_burned_c6` gates eq. 58 on `RSC > RSS`, which is eq. 65's condition and is **not** part of eq. 58 — so C6's published CFB is zero where a crown fire would be slower than the surface fire feeding it; `crown_rate_of_spread_c6` computes eq. 59 (crown flame temperature, `tt`) and never reads it, dead code at 1.9.2 and at `4d20a30`; and it computes eq. 60 (heat of ignition, `H`) and then re-inlines that expression as eq. 61's denominator rather than using `H`. Also corrected in passing: upstream's comment on eq. 61 reads "Average foliar moisture effect", which belongs to the `FMEavg <- 0.778` line above it. **`crownChangesROS` is untouched.** It still excludes C6 from the six tests that assert this package's surface `ROS` against the fixture's blended `ros` column, which is correct and stays correct — `ROS`'s signature has nowhere to put FMC/SFC/CBH/CFL, so it cannot become C6-aware. The coverage those exclusions were withholding is now in `TestCFFDRSC6RateOfSpread` instead, which is why narrowing `TestCFFDRSCrownFractionBurned`'s exclusion is still the open question it was. No default CBH, CFL or FMC was added. |
| `total_fuel_consumption.r` | CFC, TFC | ✅ | `consumption.go` `CrownFuelConsumption`, `TotalFuelConsumption` — eqs. 66a/66b/66c (Wotton 2009, GLC-X-10) and eq. 67 (FCFDG 1992). **Both equation numbers this row used to carry were wrong**: it said "TFC's surface half (eq. 60)", and eq. 60 is C6's heat of ignition — see `c6.go`. Ported 2026-09-20, oracle case **(b)**: `CFC` and `TFC` were in `fbp()`'s contract and simply not emitted. The second blocker was indeed never real — `total_fuel_consumption` takes CFL as a *parameter* and this package keeps it the caller's, so no per-fuel table was needed or added. What the row did not predict is the **CFL sweep problem**: every crown-block row sends `CFL = 1.0`, so the new columns would have been asserted by 10752 rows that an implementation ignoring eq. 66a's CFL factor entirely would also have passed. The generator grew a 512-row block sweeping CFL over four values in (0, 2] — the range `crown_fuel_load` honours verbatim — and PC and PDF across both mixedwood weightings. 11264 cases assert at **worst relative error 0** from the oracle's own CFB and SFC; 11186 more (the surface blocks included) assert the table-free identity that a zero CFB gives `cfc` = 0 and `tfc` = `sfc`. HFI is the row below, and it is now unblocked. |
| `fire_intensity.r` | HFI | 🔴 | — **unblocked 2026-09-20**: TFC went ✅ that day, and that was the only real dependency edge among the 🔴 rows. Eq. **69** (FCFDG 1992), not the eq. 66 this row used to cite — 66 is CFC. Oracle-confirmed: `fire_intensity(FC, ROS) = 300·FC·ROS`, called as `HFI <- fire_intensity(TFC, ROS)`. This is the only real dependency edge among the 🔴 rows. `HFI` is in `fbp()`'s contract but is not emitted. For C6 the ROS factor is the crown-blended rate, which `C6CrownFire` now returns (`C6calc.r` went ✅ on 2026-09-19), so nothing blocks a C6-inclusive HFI on flat ground. On a slope it inherits `Slopecalc.r`'s C6 gap — see that row. |
| `rate_of_spread_at_time.r`, `distance_at_time.r`, `length_to_breadth_at_time.r` | acceleration model | 🔴 | — the whole time-dependent branch. Nothing here is time-aware. **Not blocked by any 🔴 above it** (queried 2026-09-18): `alpha` depends on fuel type and CFB (✅) alone, and the inputs are ROS/BROS/LB (✅). It is blocked by the *sweep*: `fbp()` gates every t-column on `ACCEL`, and the generator pins `Accel = 0` and `hr = 1` on all 24,260 rows, so `HROSt`/`FROSt`/`BROSt`/`LBt`/`DH`/`DB`/`DF`/`TI` currently all carry their equilibrium values. Reaching it needs an `Accel = 1` block and an `hr` sweep — the largest generator change of any 🔴 row, and the digest moves. Watch `HR <- HR*60`: hours in, minutes in the model. |
| `fbp.r`, `fire_behaviour_prediction.r` | the umbrella driver | 🟡 | — gofbp exposes the pieces, not one `fbp()`-shaped call. Deliberate for now; revisit only once the 🔴 rows above close, since a driver that silently defaults FMC/CBH/CFL is exactly the local judgement this package refuses to make. (SFC and FMC were both in that list until 2026-09-18 and are now ✅, so they are the two a driver could be handed rather than invent; CBH and CFL are what remain.) Upstream's own driver shows what is being refused: `fbp()` called with **no input at all** returns a complete prediction, defaulting everything from hard-coded per-fuel `CBHs`/`CFLs` vectors. |
| `fine_fuel_moisture_code.r` | FFMC — the FWI System's fine fuel moisture code, one day | ✅ | `fwi/fwi.go` `FFMC` — Van Wagner (1987) eqs. 1-10, asserted over 7,560 direct calls to the unexported component at **worst relative error 1.37e-15**, plus every `fwi()` row below. Reaches both rain arms (eq. 3a, and 3b below yesterday's FFMC ≈ 20.0), the 0.5 mm threshold from both sides, the 250 % moisture cap, drying, wetting, the band between the equilibria, RH 0 and exactly 100 (unclamped — only the component sees it), zero wind, and the 101 clamp. **The 101 clamp binds materially only above about 56.6 °C (measured on a 0.1 °C × 0.1 % grid), where the drying equilibrium goes negative; otherwise all it absorbs is a rounding ulp** — an unchanged FFMC of 101 round-trips eqs. 1 and 10 as 101.00000000000001. **The 0 clamp is unreachable** (it needs the wetting equilibrium above 250 %, i.e. about −1200 °C) and is not claimed as covered: `TestFFMCIsBoundedWithoutItsClamps` checks both statements against the unclamped arithmetic. Departures from 1987: the exact 250·59.5/101 for 147.2, and the [0, 101] clamp. Upstream's comments label the wetting-rate block "Eq. 6a … drying rate" and the drying block "Eq. 7a … wetting rate" — the paper's 6a is the drying rate, the one using RH/100; the code is right and the comments are swapped. |
| `duff_moisture_code.r` | DMC, with `fwi()`'s latitude-adjusted day length (`lat.adjust`) | ✅ | `fwi/fwi.go` `DMC`, `DMCDayLength` — eqs. 11-17 and the five `lat.adjust` bands, asserted over 1,350 direct calls at **worst relative error 0**: every month in every band, both sides of every boundary (30, 10, −10, −30, −90), the 1.5 mm threshold, all three of eq. 13's pieces with 33 and 65 on the grid, the −1.1 °C floor, the `pr < 0` floor (heavy rain on a near-zero DMC), and the final floor (RH above 100, which only the component can be sent). **Three things about the bands the ledger should carry:** exactly 30° is the 20 °N band (upstream's comment says 46 °N applies "latitude >= 30N"; the code says > 30); a latitude below −90 matches no adjusted band and falls through to the northern table; and `lat.adjust = FALSE` is the > 30° band everywhere, so `fwi` has no flag for it — its rows are asserted at 46 °N instead. **One cffdrs departure has a measured cost:** the "altered" eqs. 12 and 15 (`280/exp(0.023·P)`, `43.43·(5.6348 − ln(M − 20))`) are not inverses the way the published pair are — they round 1/43.43 to 0.023 and exp(5.6348) = 280.0029 to 280 — so every day with rain above 1.5 mm takes about 0.11 % more off the DMC than the paper would. Reproduced, because matching cffdrs is the point, and written into `DMC`'s doc comment. |
| `drought_code.r` | DC, with `lat.adjust` | ✅ | `fwi/fwi.go` `DC`, `DCDayLength` — eqs. 18-23 and the three `lat.adjust` bands, asserted over 1,224 direct calls at **worst relative error 3.92e-16**: every month in every band with both sides of ±20, the 2.8 mm threshold, the −2.8 °C floor, the evapotranspiration floor (January at 0-1 °C), and the rain floor (100 mm on a low DC). The DC bands do not share the DMC's boundaries — 15 °N is the DMC's 20 °N band and the DC's equatorial one — and a chain runs there. DC does not read RH; cffdrs takes it anyway, so does `DC`, and the grid sends both extremes so a port that used it would fail. The final `dc1 < 0` floor is unreachable from a non-negative yesterday and is not claimed. Eq. 21 is rearranged to `DC − 400·ln(1 + 3.937·rw/Q)`, which is the published algebra, not a departure. |
| `initial_spread_index.r` (`fbpMod = FALSE`) | ISI, the FWI System's form — what `fwi()` computes | ✅ | `fwi/fwi.go` `ISI` — eqs. 24-26 with `exp(0.05039·wind)` at every speed, asserted over 176 direct calls at **worst relative error 4.37e-16**, the wind grid running to 150 km/h so a port that borrowed `fbp.ISI`'s high-wind form would fail every row above 40. FFMC 0 is computed like any other value; `fbp.ISI` returns 0 for it. **Not shared with `fbp.ISI` on purpose** — they are different equations that share a name and a file, and each doc comment names the other. |
| `buildup_index.r` | BUI | ✅ | `fwi/fwi.go` `BUI` — eqs. 27a/27b, asserted over 99 direct calls at **worst relative error 0**: DMC = DC = 0 (the 0/0 guard), DMC = 0 with DC > 0, both sides of DMC = 0.4·DC, and eq. 27b's floor. Two properties are pinned by invariant tests rather than left to be found: BUI is 0 whenever DMC is, and BUI is **not** monotone in DMC when DC is below about 1.15 (eq. 27b's slope just past DMC = 0.4·DC is 1 − 1.25·cc/DC). |
| `fire_weather_index.r` | FWI | ✅ | `fwi/fwi.go` `FWI` — eqs. 28-30, asserted over 90 direct calls at **worst relative error 1.07e-15**, with BUI at 79.9, 80 and 80.1 and both the `bb <= 1` identity and the log-power form. **FWI is not monotone in BUI, and that is the paper:** eq. 28a gives fD = 23.686 at 80 and eq. 28b 23.666 just above, a 0.08 % step down. `TestFWIStepsDownAtBUI80` pins it so a "fix" is caught as the departure from cffdrs it would be. |
| `fwi.r` | `fwi()`'s per-day step — RH clamp, the six codes chained, DSR — and its default start-up codes | ✅ | `fwi/step.go` `Step`, `State`, `Weather`, `Indices`, `StartupState`; `fwi/fwi.go` `DSR`, `StartupFFMC`, `StartupDMC`, `StartupDC` — asserted three independent ways: 3,420 one-day `fwi(batch = FALSE)` rows (every weather extreme across nine latitudes and four months, plus 360 at `lat.adjust = FALSE`), **worst relative error 3.43e-15**; ten multi-day chains of 40-120 days through `fwi()` itself, 715 days, with the Go carrying only its own state from day 1 (**1e-14** — the chains are contractive, so an ulp does not grow); and cffdrs' own `test_fwi` sample, 48 days from the default start-up codes (**3.66e-15**). The one thing `fwi()` does that the components do not — **RH ≥ 100 becomes 99.9999 before any code sees it** — is in `Step` and nowhere else, and 360 rows reach it. DSR (`0.0272·FWI^1.77`, upstream "eq. 31") has no function upstream; these rows are its only oracle. **Deliberately not ported, and the reason is the row's scope rather than a gap:** the loop over days, `batch`'s sorting of stations by date, and `fwi()`'s silent defaults — 55 °N for a missing latitude, July for a missing month, 85/6/15 for a missing init. The first two are the caller's time series; the defaults are exactly the local judgement this module refuses (the start-up codes are exported constants the caller passes, not values `Step` assumes). `fwi()`'s `stop()` on negative precipitation, wind or RH is `Step` returning NaN in every field. |
| `gfmc.r`, `grass_fuel_moisture*.r`, `hffmc.r`, `hourly_fine_fuel_moisture_code.r`, `sdmc.r` | hourly/grass/sheltered-duff moisture codes | ⚪ | Separate models, not the daily FWI System `fwi` ports. Each has its own time step or inputs — the hourly FFMC steps hour by hour from hourly weather, grass fuel moisture also takes solar radiation (`isol`), and the sheltered DMC is a duff model of its own — so none is an extension of `Step`, and `fwi` stops at the seven outputs `fwi()` returns. Porting one would be a new row with its own oracle block. No consumer has asked for one. |
| `fire_season.r`, `overwinter_drought_code.r` | seasonal bookkeeping | ⚪ | Decisions about where a chain of days starts, and `fwi` makes none: `Step` has no season logic and the start-up codes are exported for the caller to choose. `fire_season` decides a season's start and end from temperature and snow; `overwinter_drought_code` decides a spring DC from last autumn's and the winter's precipitation, through two coefficients (`a`, `b`) that are site judgements. Both belong with the caller's season bookkeeping, next to the time series. |
| `fbpRaster.r`, `fwiRaster.r`, `gfmcRaster.R`, `hffmcRaster.r` | raster wrappers | ⚪ | Gridding is the caller's; both packages stay dependency-free and scalar. `fwiRaster` is `fwi()` per cell, which `Step` already is. |
| `lros.r`, `pros.r` | line/point ROS from observed arrival times | ⚪ | Inference from observations, not the FBP System's forward equations. |
| `cffdrs-package.R` | roxygen package docs | ⚪ | Not code. |

## Non-`R/` directories

| Upstream | Status | Note |
|---|---|---|
| `data/` | 🟡 | **Both halves of this row were wrong until 2026-09-18, and the oracle says so.** `data(package="cffdrs")` lists exactly nine `test_*` sample input sets — `test_fbp`, `test_fwi`, `test_gfmc`, `test_hffmc`, `test_lros`, `test_pros`, `test_sdmc`, `test_wDC`, `test_wDC_fs` — and **no fuel-type table**. The per-fuel CBH/CFL defaults do **not** live here either: they are hard-coded vectors inside `R/crown_base_height.r` and `R/crown_fuel_load.r`, which carry their own 🔴 rows above, so this row was also double-counting them. gofbp transcribed the ST-X-3 fuel tables by hand from the publication and checks them against the fixture; there is no upstream data object to port them from. Left 🟡 rather than moved — with nothing here to port, ⚪ is arguable, and that is a status call. **Since 2026-09-24 one of the nine is consumed:** the generator runs `test_fwi` through `fwi()` as `TestCFFDRSSampleDataset`'s input. That is use as a test input, not a port, so the status argument is unchanged; it does mean `test_fwi` changing upstream would move a reference number. |
| `tests/` | 🟡 | Not mirrored. gofbp asserts against a generated 24,260-case sweep instead (`testdata/gen_cffdrs_reference.R`). Upstream's own test cases are still worth reading for edge cases the sweep does not reach — but note R does not install `tests/`, so they are not readable from the pinned container and need the upstream tree. What *is* installed and readable is `inst/extdata` (see below), which pairs with `data/test_fbp` as upstream's own FBP regression case. |
| `man/`, `inst/` | ⚪ | Docs and package metadata — **true of `man/`, and not true of `inst/`.** Queried 2026-09-18: the installed package ships `inst/extdata/test_fbp_out.Rdata`, a 20×9 table of upstream-published FBP reference output (`ID, CFB, CFC, FD, HFI, RAZ, ROS, SFC, TFC`) — the same quantities gofbp asserts, plus the TFC/HFI it still owes — alongside raster fixtures. That is the same content class as the 🟡 `tests/` row, sitting under a reason that says there is nothing here. Splitting `inst/` out of this row moves content out of ⚪, so it is left for a human; see the open question above. |
| `NEWS.md` | — | **Read on every version bump.** It is the cheapest signal that a coefficient moved. |

## Concepts still missing, in dependency order

The 🔴 rows are not independent. Doing them out of order means writing code that
cannot be oracle-tested yet.

> **Measured 2026-09-18: this is not a dependency order.** The call graph was
> queried against the pinned oracle rather than read, and of the edges this list
> asserts, only two exist:
>
> | stated edge | real? | why |
> |---|---|---|
> | CBH/CFL before C6 | **no** | C6's three functions take `ISI, FMC, RSS, RSO, CFB` — no table. **Confirmed by porting it on 2026-09-19 against the fixture already on disk.** |
> | CBH/CFL before a complete TFC | **no** | `total_fuel_consumption` takes CFL as a *parameter*; `fbp()` substitutes the table before the call |
> | TFC before HFI | yes | `HFI <- fire_intensity(TFC, ROS)` |
> | everything → the driver | yes | by definition |
> | acceleration | independent | blocked by the sweep only. FMC was the other one, ✅ 2026-09-18; C6 was the third, ✅ 2026-09-19 |
>
> So the sentence above — "doing them out of order means writing code that cannot
> be oracle-tested yet" — is **false for item 2, TFC/HFI**, which can be
> oracle-tested today once the generator emits the columns. What remains is a cost
> and design order, not a correctness one.
>
> **The list is deliberately left in its current order anyway.** Item 2's own note
> says promotion "is a scheduling call for a human, not a correctness one", and
> that reservation outlives the finding that produced it: the audit's job is to say
> the edges are not real, not to decide what to do about it. The ordering argument
> in the other direction has now been taken: C6's ROS feeds HFI (`300·TFC·ROS`) and
> the acceleration model, so it went first and neither will need revisiting to lift
> its exclusion. Item 1's "ask before choosing" still gates any CBH/CFL work
> regardless of position, and is the reason the 2026-09-19 run took C6 rather than
> the row above it — an unattended run may not make that decision.

Each item names the upstream file its row is keyed on, because `TestLedger…` in
`ledger_test.go` joins this list to the table above on exactly that name — every
🔴 row has to appear here, and nothing may appear here that is not still owed.

1. **CBH / CFL defaults** (`crown_base_height.r`, `crown_fuel_load.r`) — per-fuel
   tables; small, and the last thing standing between `Crown` and a
   fuel-code-only call. **How these are exposed is a design decision, not a
   porting one** — a lookup the caller opts into is a different package from a
   silent default inside `Crown`. Ask before choosing. Note the exposure decision
   is now the *only* thing blocking this row: both tables were recovered verbatim
   from the pinned oracle on 2026-09-18 (CBH by inverting eq. 56, CFL as
   `CFC/CFB`), so "no column" was true and "no oracle" was not.
2. **HFI** (`fire_intensity.r`) — eq. 69, `HFI = 300·TFC·ROS`. Total fuel
   consumption went ✅ on 2026-09-20, and that was the one real dependency edge
   in this list, so nothing blocks this but a generator change: `HFI` is in
   `fbp()`'s contract and is not emitted, so adding it moves the digest. For C6
   the ROS factor is the blended rate, which `C6CrownFire` returns; on a slope it
   inherits the C6 gap recorded on the slope row above. **This item used to read
   "TFC, HFI" and it was two rows wearing one name** — they are separate rows in
   the table above, with separate blockers. The 2026-09-20 port took the first
   and left this one, which is what /migration-port's one-row rule asks for. Left
   in position rather than promoted, for the reason it always was: moving it is a
   scheduling call for a human.
3. **Acceleration model** (`rate_of_spread_at_time.r`, `distance_at_time.r`,
   `length_to_breadth_at_time.r`) — the largest remaining block, and the only one
   that changes the package's shape (it introduces time).
4. **An `fbp()`-shaped driver** (`fbp.r`) — last, or never. See the row above.

## Log

Newest first. One line per audit; a day with no change still gets a line, so a
gap in the dates is visible as a gap.

| Date | Upstream commit | What changed |
|---|---|---|
| 2026-09-24 | `4d20a30` | Upstream unchanged (`tools/upstream-drift` exit 0: HEAD still `4d20a30`, so no `NEWS.md` versions to read and every pin re-dated on measurement rather than carried). **The scope boundary moved, deliberately, and package `fbp` did not.** The FWI System was ⚪ — "inputs to FBP, not part of it" — and that stays true of package `fbp`: it is still the FBP System and nothing else, and not one of its numbers moved. What changed is that the *module* now ships a second package, `fwi` (`fwi/`), because a downstream consumer computing FFMC/DMC/DC/ISI/BUI/FWI from raw weather (MEPS forecasts, ERA5-Land) needs an implementation it can trust against an oracle, and this repository is where the pinned cffdrs oracle already is. A separate repository was considered and rejected: it would have duplicated the container, the pins, the generator, the ledger and every tool that reads them, and two oracles pinned in two places is how pins drift. `fbp` and `fwi` import `math` and nothing else — each other included — which `TestPackageHasNoDependencies` now checks for both; the only code they both have is a constant (250·59.5/101) each carries privately. `fbp.ISI` and `fwi.ISI` are **not** merged: they are `initial_spread_index.r`'s two branches (`fbpMod = TRUE`/`FALSE`), different wind functions above 40 km/h, and each doc comment names the other. **Ported, ⚪ → ✅ with no 🔴 in between (the fixture came first):** `fine_fuel_moisture_code.r`, `duff_moisture_code.r`, `drought_code.r`, `initial_spread_index.r` (`fbpMod = FALSE`), `buildup_index.r`, `fire_weather_index.r` and `fwi.r`'s per-day step, as `FFMC`, `DMC`, `DC`, `ISI`, `BUI`, `FWI`, `DSR`, `DMCDayLength`, `DCDayLength`, `Step` and the start-up constants. Oracle case **(b)** at a scale no row has needed before: the generator grew a whole second block, emitted under a new top-level `fwi_cases` key so that no FBP row changed — **14,682 FWI cases**: the six unexported components called directly through `:::` over grids that reach every branch (the rain thresholds, verified in the source as FFMC > 0.5, DMC > 1.5 and DC > 2.8 mm with ≤ on the dry side, each on and just above the line; both FFMC rain arms and its drying/wetting/neither paths; the −1.1 and −2.8 °C floors; RH 0 and 100; zero and 150 km/h wind; every month; every `lat.adjust` band of both codes with both sides of every boundary; `lat.adjust = FALSE`), 3,420 one-day `fwi()` rows, ten 40-120-day chains through `fwi()` (715 days, Swedish stations at 55.7, 62.4 and 67.9 °N among them), and cffdrs' own `test_fwi`. Nine `TestCFFDRS*` assert it; the worst relative error anywhere is **1e-14** (a 120-day chain) and most are 0-4e-16, against the repo-wide 1e-9 — no test needed a looser one. **Digest `4d80079d…b2515e` → `e438b1d3…865387`; `tools/fixture-diff` over the pre-FWI copy: *No column shared by both fixtures moved*, exit 0 — 24,116 shared FBP cases, 0 only-in-old, 0 only-in-new,** and the file is byte-identical up to the end of `cases`. The FBP case count is still 24,260; the FWI rows carry no `fuel` key, so `precheck`'s count and the four documents that state it are untouched. **cffdrs quirks found, all now written into the doc comments and rows above:** `fwi()` clamps RH ≥ 100 to 99.9999 before any code — the components do not, so `Step` does and `FFMC`/`DMC` do not; the "altered" DMC rain equations are not inverses and cost ~0.11 % of DMC per rain day against the paper; the DMC band boundary is > 30 where upstream's comment says ≥ 30; latitudes below −90 fall through to the northern table; upstream's FFMC comments swap the drying and wetting equation labels; FFMC's 101 clamp is live only above ~56.6 °C and otherwise absorbs a rounding ulp, and its 0 clamp is unreachable. Two properties of the *published* equations pinned rather than "fixed": FWI steps down 0.08 % crossing BUI 80, and BUI is not monotone in DMC below DC ≈ 1.15. **The tools learned the second package:** `internal/ledger` reads a row variant (`` `initial_spread_index.r` (`fbpMod = FALSE`) ``) so one file can back two rows, with `ByFile` returning both and `ByKey` telling them apart; `tools/upstream-drift` reports a change to such a file once per row; `tools/precheck` counts `TestCFFDRS*` across `./...`; `tools/fixture-diff` diffs `fwi_cases` as its own section; the ledger tests glob `fwi/` too; the Capslock gate covers `./fwi`; and the ten `errcheck` findings `golangci-lint` already reported on `main` (all in `tools/`, all deliberately ignored returns) are now explicit `_ =`. **Left ⚪, reasons rewritten** because "not part of FBP" no longer explains them in a module that ships `fwi`: the hourly/grass/sheltered-duff models (separate models with their own time step or inputs), `fire_season.r`/`overwinter_drought_code.r` (season-start decisions the caller makes), and the raster wrappers (gridding is the caller's). **Not ported from `fwi.r`, deliberately:** the day loop, station batching and `fwi()`'s silent defaults (55 °N, July, 85/6/15) — the time series is the caller's, and the start-up codes are exported constants rather than defaults. |
| 2026-09-20 | `4d20a30` | Upstream read at the pin for this row's two files only; no drift check run (this was a `/migration-port` run, not an audit), so the **upstream package-version pin is carried forward rather than re-measured** — its date moves with the rest because `TestLedgerLogIsContiguous` requires that, and this sentence is what says it was not checked. **The gate was red on arrival and the fixture was the reason**: this worktree carried `148a5a9e…24cf8f`, 23532 cases, the digest the table in `testdata/README.md` records as predating the FMC block — so the two FMC tests failed rather than skipped, exactly as that table predicts. Regenerating from the pinned container reproduced `e0e51ff1…7db8cc` byte for byte and `precheck` went exit 1 → 0. **Ported total fuel consumption** — `total_fuel_consumption.r` → `consumption.go`, eqs. 66a/66b/66c and 67, 🔴 → ✅. Oracle case **(b)**: the sweep is **23,748 → 24,260** cases and the digest **`e0e51ff1…7db8cc` → `4d80079d…b2515e`**. **`tools/fixture-diff` over the pre-port copy: *No column shared by both fixtures moved*, exit 0** — 23,604 shared cases, 0 only-in-old, 512 only-in-new, every output column unchanged including `cfb` and `sfc`, the two this row composes. **The generator grew a block, and the reason is the `fmc` trap in a new costume.** `CFC` and `TFC` needed only two emit lines, but eq. 66a is `CFL·CFB` and every crown-block row sends `CFL = 1.0` — so the new columns would have been asserted by 10752 rows that an implementation ignoring the CFL factor entirely would also have passed. The column existed and would not have exercised the function. The 512-row block sweeps CFL over 0.3/0.8/1.6/2.0, inside the (0, 2] range `crown_fuel_load` honours verbatim — outside it fbp() silently substitutes the table and the fixture records what was *sent*, which `usableForConsumption` now guards. Four passes: **11264** cases from the oracle's own CFB and SFC at **worst relative error 0**; the same 11264 with SFC computed here (2.16e-16); **5888** flat ones end to end from FFMC and wind through our SFC, surface rate and CFB (2.39e-15); and **11186** cases — the surface blocks included — carrying the table-free identity that a zero CFB gives `cfc` = 0 and `tfc` = `sfc` exactly. That fourth pass is deliberately not a back-door recovery of the CFL table: it holds for *any* CFL, which is why it can run on rows whose CFL the fixture cannot say. **Two R-vs-paper notes.** Eq. 67 is FCFDG 1992's but CFC is not: cffdrs cites 66a/66b/66c to Wotton, Alexander & Taylor (2009), GLC-X-10 — the same revision that replaced eq. 9 with 9a/9b for C1 — and 66c could not have been in the 1992 publication at all, since M3 and M4 are GLC-X-10's own fuels. And an unknown fuel code returns 0 here where cffdrs' `ifelse` chain falls through to the unweighted product; that is this package's convention, matching `SurfaceFuelConsumption`, and it is written into both doc comments. **Two equation numbers in this file were wrong and are corrected**: the TFC row said eq. 60 (that is C6's heat of ignition) and the HFI row said eq. 66 (that is CFC); they are 67 and 69. **Dependency-order item 2 was two rows wearing one name** — `/migration-port` says to port the first and say so, so TFC went and HFI stayed, now unblocked and alone in that item. **Item 1, CBH/CFL, was not taken and not decided.** Its exposure question is on "Escalate rather than decide" and an unattended run may not answer it; this run needed no table and added no default, and `Crown.CFL` is still the caller's field. |
| 2026-09-19 | `4d20a30` | Upstream unchanged at the pin; no drift check run (this was a `/migration-port` run, not an audit). Gate green after regeneration — the fixture was **absent** in this worktree, `./testdata/regen-cffdrs.sh` rebuilt it from the pinned container and it reproduced `e0e51ff1…7db8cc` byte for byte, so `precheck` went exit 1 → 0 (0 of 18 `TestCFFDRS*` skipping). **Ported C6's crown rate of spread** — `C6calc.r` → `c6.go`, eqs. 61/64/65 plus eq. 58's C6 gate, 🔴 → ✅. Oracle case **(a)** — and the command's "there is no case (a) row left" is therefore withdrawn: C6's `ros`/`cfb` columns already were the blended quantity, so the generator did not change, the sweep is still 23,748 cases and `tools/fixture-diff` over the pre-port copy reports *No column shared by both fixtures moved*, exit 0, every output column unchanged. Unlike `fmc`, the column also **exercises** the function: the crown block varies FMC through LAT × Dj over 85.17 … 120 and ISI through FFMC × WS × GS, so eq. 64's 0.0497 and eq. 61's four coefficients are all reached. Asserted in three passes — eq. 65 fed the oracle's own CFB over all **948** C6 rows (worst relative error 1.03e-15); the whole chain from the oracle's ISI over the **768** crown-block rows (ROS 1.63e-15, CFB 1.22e-15); and end to end from FFMC and wind over the **384 flat** crown-block rows (1.63e-15). 400 of the 768 crown. Eqs. 62 and 63 needed no code: they are the generic RSI curve at C6's table row and `RSI("C6", …)·BuildupEffect("C6", …)` already returned them. **The port found a gap in a different row, and it is why the end-to-end pass is flat-only.** `slope_adjustment` inverts the zero-wind rate from `rate_of_spread(FUELTYPE, ISZ, …)`, which for C6 is the **blended** rate — so cffdrs' slope-equivalent wind for a sloped C6 stand moves with FMC and CBH (fixture, GS 30 / WS 0 / FFMC 95 / BUI 100: `isi` 15.930431 at FMC 85.1701 against 11.328856 at FMC 120), and `NetEffectiveWind`, which is surface-only for every fuel, cannot reproduce it. So `Slopecalc.r`'s ✅ is true of sixteen fuels and silent about the seventeenth, `TestCFFDRSSlopeBackSolve`'s C6 exclusion is load-bearing, and porting RSC did not lift it. Recorded on that row with the numbers; whether it moves to 🟡 is parked above as a status call. **Two branches the sweep cannot reach, both stated rather than left to look covered.** RSC exceeds RSS in all 948 rows, and that is a property of the equations: RSS's supremum over all ISI and BUI is 30·BE_sup = 35.9149 m/min and RSC's is 60·FME/FMEavg, and they cross at FMC **127.26**, above the 120 eq. 8 caps foliar moisture at — so the `RSC > RSS` gate is unreachable from the model's own FMC, which `TestC6CrownGateIsUnreachableWithinTheFMCRange` pins and the unconditional tests assert directly. cffdrs' `ROS <= 0 → 1e-6` floor is likewise unreached (smallest C6 rate in the sweep 1.70913e-08, positive) and is not reproduced. **Three R-vs-paper departures written down, one of them substantive:** `crown_fraction_burned_c6` gates eq. 58 on `RSC > RSS`, which is eq. 65's condition and not eq. 58's, so C6's published CFB is zero wherever a crown fire would be slower than the surface fire feeding it; eq. 59's crown flame temperature `tt` is computed and never read, dead at 1.9.2 and at `4d20a30`; and eq. 60's heat of ignition `H` is computed and then re-inlined rather than used. Upstream's comment on eq. 61 also mislabels it "Average foliar moisture effect", which is the `FMEavg <- 0.778` line above. **`crownChangesROS` and `TestCFFDRSCrownFractionBurned` are untouched on purpose** — `ROS`'s signature has nowhere for FMC/SFC/CBH/CFL, so it cannot become C6-aware, and the coverage those exclusions withheld now lives in `TestCFFDRSC6RateOfSpread` instead; narrowing the pass-two exclusion stays the open question it was. No default CBH, CFL or FMC was added, and the row above C6 in the dependency order was left alone: its exposure decision is on "Escalate rather than decide", which an unattended run may not make. |
| 2026-09-18 | `4d20a30` | Upstream unchanged; no drift check run (this was a `/migration-port` run, not an audit). **Ported FMC** — `foliar_moisture_content.r` and `foliar_moisture_content_minimum.r` → `foliar.go`, eqs. 1-8, 🔴 → ✅. Oracle case **(b)**: the `fmc` column existed but `LONG`, `ELV` and `D0` were constants at 15, 0 and 0, so the fixture reached exactly one of the model's three paths — eqs. 3/4 and the caller-supplied-`D0` branch had no coverage and eqs. 1/2's longitude exponential was pinned at one point. The generator grew a 216-row FMC block (18 sites × 12 dates) and those three input columns; the sweep is **23,532 → 23,748** cases and the digest **`148a5a9e…24cf8f` → `e0e51ff1…7db8cc`**. **`tools/fixture-diff` over the pre-port copy: *No column shared by both fixtures moved*, exit 0** — 23,388 shared cases, 0 only-in-old, 216 only-in-new, and every output column (`fmc` included) unchanged. 17,364 cases assert FMC at **worst relative error 0**; 6,384 are excluded by name because `fbp()` zeroes FMC for D1/S1/S2/S3/O1A/O1B *after* computing it, which is the driver's decision and not eqs. 1-8's. Three places the R departs from FCFDG 1992, all now written down and two of them newly oracle-backed: cffdrs branches on `ELV <= 0` where the paper branches on "elevation known", so a measured 0 m gets the missing-data equations; cffdrs **rounds D0** (the paper does not) with R's half-to-**even** rule, which `math.Round` would get wrong — sites at `D0 = 150.5` and `151.5` now separate the two rules and the oracle sides with half-to-even; and `fbp()` folds the sign of `LONG` before calling, which one deliberately negative-longitude site pins. **The 1.10.0 provenance question did not block this and is still open** — the two versions' FMC source was read side by side and is the same arithmetic, with one named exception now recorded in the drift note above. No default for FMC was added; `Crown.FMC` is still the caller's field. **One claim from the same day's `/migration-sweep` is withdrawn by this port:** that `foliar_moisture_content_minimum`'s absence at the pin made "half this row unportable against 1.9.2 by construction". The absence is real, the consequence is not — 1.9.2 carries eqs. 1-4 inline, and that inline code is byte-for-byte what HEAD moved into the second file. Merged with `main` after that sweep landed; the dependency order lost its item 1 and every cross-reference to a number in it shifted up one. |
| 2026-09-18 | `4d20a30` | Upstream unchanged (`upstream-drift` exit 0); gate green (`precheck` exit 0, 0 of 14 skipping, digest `148a5a9e…24cf8f`); no port, no generator change, no regeneration. **First `/migration-sweep` — the judgement half fanned out across the five status groups, every verdict backed by a query against the pinned container rather than a reading.** It found more wrong than the daily pass had, and the pattern is one thing: *claims about upstream that nobody had ever asked upstream about.* **The dependency order is not a dependency order** — of its stated edges only TFC→HFI and "driver last" exist. C6 does **not** depend on CBH/CFL (`crown_rate_of_spread_c6`/`crown_fraction_burned_c6`/`rate_of_spread_c6` take `ISI, FMC, RSS, RSO, CFB` and no table), and TFC does not either (`total_fuel_consumption` takes CFL as a *parameter*; `fbp()` substitutes the table before the call). Both rows' blockers are withdrawn. The list is left in its order anyway — item 4 reserves promotion for a human, and that survives the finding. **C6 is the top unblocked 🔴 and needs no regeneration**: 768 of its 948 fixture cases are crown-block rows whose `ros`/`cfb` already are the blended quantity. "Every oracle test excludes C6 by name" was false — six do, and `TestCFFDRSCrownThreshold` includes it deliberately. **CBH/CFL have no column but do have an oracle**: both published tables were recovered verbatim (CBH by inverting eq. 56, CFL as `CFC/CFB` net of the M1/M2 `PC/100` and M3/M4 `PDF/100` weightings), so only the exposure decision blocks them. **`foliar_moisture_content_minimum` does not exist at the pin at all**, which makes half that row unportable against 1.9.2 by construction rather than unswept. **`data/` was wrong twice over** — nine `test_*` sample sets, no fuel-type table, and the CBH/CFL defaults are vectors in the R sources, not data objects, so the row was double-counting two 🔴 rows. **`inst/` is not "docs and metadata"**: `inst/extdata/test_fbp_out.Rdata` is a 20×9 table of upstream-published FBP reference output. The 🟢 `direction.r` row survived on narrower ground than it claimed — `pros()`/`lros()` *are* exported and *do* return a `Direction` column, so "no oracle column exists" is false as stated, but it is not an oracle for `AngleBetweenDeg`, which is a different function (`.direction(10,40,15)` = `.direction(40,10,15)` = 25 where `AngleBetweenDeg(10,40)` = 30). Among the exclusions: `crownChangesROS` is a sound mechanism and has only ever narrowed, but `TestCFFDRSCrownFractionBurned`'s C6 reason was **false about upstream** — C6's CFB is eq. 58 on the surface rate like every other fuel, RSC only gates — and its pass two excludes 768 rows that match exactly, so that exclusion hides agreement; the comment is corrected here, the exclusion is left alone as a decision. Three exclusions are dead (`c.ROS == 0` cannot fire — upstream floors ROS at 1e-6 and the fixture's minimum is 3.26e-10 but never 0; `nearBoundary` skips 0; and a silent `angleName` lookup miss with no comment at all), and one has widened to remove 4,992 rows that all pass the contract it also silences. Yesterday's `rate_of_spread_at_theta.r` note was re-verified adversarially and every sub-claim and all four numbers held; corrected only in that it understated the propagation (`TTFC` too) and quoted outputs without naming the inputs, which cost the auditor a 136,000-row grid search — the case is now named in the row. Four status/scope calls are parked above rather than taken. |
| 2026-09-18 | `4d20a30` | Upstream unchanged (`tools/upstream-drift` exit 0: HEAD still `4d20a30`, so there are no `NEWS.md` versions between the pin and now); no port. Gate green (`precheck` exit 0: READY, `go test ./...` green, 0 of 14 `TestCFFDRS*` skipping, fixture `148a5a9e…24cf8f`). **Targeted sweep on the two rows the previous audit left open — and the first overturns that audit's own correction.** `rate_of_spread_at_theta.r` was measured against the pinned container this time rather than read, and the row had conflated two different upstream quantities: `fbp()`'s `TROS` (`ROS·(1-E)/(1-E·cos(THETA-RAZ))`, built from ROS and LB only) is **not** eq. 94's `rate_of_spread_at_theta(ROS, FROS, BROS, THETA)`, which is unexported and never called by `fbp()`. **Both are numerically defective**, so the predicted "emit `TROS` and the row becomes ✅" was never on the table. `TROS` subtracts `RAZ` in degrees from `THETA` in radians inside one `cos()` (1.9.2 lines 134 / 232 / 249, and the same three lines at `4d20a30`), so it misses even the head rate — 2.835 where `ROS` is 16.149 at `RAZ` = 180° — and carries that into `TCFB`, `TFI`, `TTI` and `TROSt`. Eq. 94's two leading terms share a denominator and collapse to `ROS/cosθ`: an uncaught pole at 90° (-2.34e17, because the `ifelse(c1 == 0, …)` guard never fires — `cos(π/2)` is 6.12e-17), negative rates from 30° to 89.9°, and `ROS` returned at *both* 0° and 180° so `BROS` never comes back. `ROSAtAngle` on the same case gives `ROS` at 0° and `BROS` to seven figures at 180°. Row left 🟡 with the evidence written into it; on that evidence the honest status is 🟢, which is now an open question above — as is reading the *published* eq. 94, which was **not** checked and decides whether this is cffdrs' transcription error or the paper's. The disproved "no rate at a bearing" claim was still live in four places the previous fix missed (`ellipse.go` header and `ROSAtAngle` doc, `ellipse_test.go`, `README.md`) and in `/migration-port`, where it was the **worked example of oracle case (c)** — all five corrected, and the command now carries the lesson ("no column exists" is a claim about upstream, so check upstream for it) rather than the claim. Corrected in passing: `ROSAtAngle`'s doc credited "monotone decrease from head to back", which its own preceding paragraph and `TestROSAtAngleIsUnimodalWithTheHeadAsMaximum` both contradict. `direction.r` re-confirmed against the oracle and unchanged: `.direction` has three formals, is unexported, and is called by exactly `lros` and `pros` (both ⚪) — not by `fbp` or `fire_behaviour_prediction`. No generator change, no regeneration, digest untouched. |
| 2026-09-18 | `4d20a30` | Upstream unchanged (`tools/upstream-drift`: HEAD still `4d20a30`); no port. **First check in this worktree actually backed by the oracle.** The fixture was generated from the pinned container and reproduced `148a5a9e…24cf8f` byte for byte, so the recorded digest is now confirmed by regeneration rather than carried forward, and all 14 `TestCFFDRS*` assert instead of skipping (`precheck`: READY, 0 of 14 skipping). Three claims in this file were wrong and are corrected above. (a) **1.10.0 is not a CRAN release** — `remotes::install_version` rejects it and the archive stops at the current 1.9.2, which is exactly what the oracle pins, so the standing "open drift" was never a bump somebody had not got to; it is a question about whether reference numbers may come from git HEAD, and it is now written down as one. (b) **`rate_of_spread_at_theta.r` does have an oracle column** — `fbp(output = "ALL")` returns `TROS`; the row was 🟢 on the claim that no column exists, and is now 🟡 with the column named. (c) 1.9.2 already returns `D0`. Also fixed `testdata/regen-cffdrs.sh`: under Git Bash it handed `docker build` an MSYS-form context path (`/c/...`) with `MSYS_NO_PATHCONV` already exported for the `docker run` mount, so **every first-time image build failed on Windows** while re-running an already-built image worked — which is why it stayed hidden. Found by trying to build a 1.10.0 oracle; with the path fixed, that build ran far enough to establish (a). |
| 2026-09-18 | `4d20a30` | Upstream unchanged (`tools/upstream-drift`: HEAD still `4d20a30`). **Ported SFC** — `surface_fuel_consumption.r` → `consumption.go`, all eleven equations, 🔴 → ✅. Oracle case (a): `sfc` was already a fixture column carried as an input, so the generator did not change and the fixture is byte-identical — `tools/fixture-diff` over the pre-port copy reports *No column shared by both fixtures moved*, digest still `148a5a9e…24cf8f`. All 23,532 cases assert, worst relative error 4.01e-16. Two places cffdrs departs from FCFDG 1992 are now written down and both are oracle-confirmed: C1 uses GLC-X-10's eqs. 9a/9b, not eq. 9 (documented); C7's eq. 13 term is clamped at FFMC 70 (**not** documented upstream). The `SFC <= 0 → 1e-6` floor is reproduced from the reference implementations but the sweep never reaches it — `TestCFFDRSSurfaceFuelConsumptionFloorIsUnreached` says so out loud. No default for SFC or GFL was added; `Crown.SFC` is still the caller's field. |
| 2026-09-06 | `4d20a30` | No upstream change; no port. Turned the mechanical half of this procedure into code: `ledger_test.go` for the step-4 sweep, and `tools/precheck`, `tools/upstream-drift`, `tools/fixture-diff` for the gate, the upstream join and the reference-number diff. The digest re-baseline below was re-derived independently here and reached the same `148a5a9e…`; `tools/fixture-diff` against a rebuilt pre-M3/M4 fixture then showed no reference number moved, across the 18804 cases the two sweeps share. Sweep is 23,532 cases exactly. |
| 2026-09-06 | `4d20a30` | Upstream unchanged (HEAD still `4d20a30`); no port. Audit found three stale claims and fixed them: the fixture digest still named the pre-M3/M4 sweep (regenerated from the pinned container, byte-identical, `148a5a9e…24cf8f`), the `rate_of_spread.r` row still said 15 fuel types, and the sweep is ~23500 cases, not ~18400. Checked the `direction.r` equivalence the row asked about: it does not hold. |
| 2026-09-06 | `4d20a30` | Ledger created. Full `R/` inventory taken against upstream 1.10.0; oracle-vs-upstream version drift recorded as an open item. |
